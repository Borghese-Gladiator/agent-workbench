# CLAUDE.md

The canonical AI-onboarding and coding-conventions file for this repo is
[`AGENTS.md`](./AGENTS.md). Read it first — it is the single source of truth for
project overview, architecture, where things live, the `awb` command contract,
and the "things NOT to do" list.

This file exists only so Claude Code finds guidance at the expected path; do not
duplicate content here. Add Claude-Code-specific notes below only when they do
not apply to other agents.
