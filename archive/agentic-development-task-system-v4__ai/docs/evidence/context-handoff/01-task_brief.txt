===== stage=task_brief mode=stage-packet =====
taskId: task_LKBUdNrLQ0
contextArtifactIds: art_gJnPctRW5D
reviewerFeedback: (none)
skillText: (none)
----- PROMPT (1688 chars) -----
# Stage: task_brief

## Task
Title: Build a playable tic-tac-toe game

Request:
Build a tic-tac-toe game as a single static web app (index.html + a JS file,
no framework, no build step) that two players can play in the browser:
- A 3x3 grid the players click to place X then O, alternating.
- Detect and announce the winner (or a draw), and offer a reset.
- Use accessible, user-facing markup so a Playwright test can drive it with
  getByRole.

In the QA/validation stage, write and run a Playwright end-to-end test that
plays a full game to a win in a real browser, with video + trace ON, and
leave the recording as proof.

## Your job
Write a Task Brief: restate the request, list scope and out-of-scope items. Then add a required "## Acceptance Criteria" Markdown table with the columns `ID | Requirement | Risk (H/M/L)` — one row per testable requirement, each with a stable ID (AC1, AC2, …). These IDs are the durable contract: later stages bind their plan and proof to them, so make each criterion concrete and verifiable. Also add an "## Open assumptions / interpretation decisions" section that diffs this normalized brief against the raw request — call out every place you inferred, narrowed, or reinterpreted, so nothing the user asked for silently disappears. This brief is aspirational, not a validation record. Read-only — do not modify any files.


Context artifacts available to you (ids): art_gJnPctRW5D

## Output
Write your task_brief as clear Markdown. Then, as the LAST thing in your
response, emit a single fenced ```json block with a structured summary
of the task_brief (keys appropriate to the stage). The json block is parsed
and stored as a structured artifact.
===== stage=task_brief mode=stage-packet =====
taskId: task_Z_wdKQ82pR
contextArtifactIds: art_foqZYk64AO
reviewerFeedback: (none)
skillText: (none)
----- PROMPT (1284 chars) -----
# Stage: task_brief

## Task
Title: Add divide with divide-by-zero handling

Request:
Add a divide(a, b) function to the calc library that throws a clear error on divide-by-zero. Follow the existing style in src/calc.js.

## Your job
Write a Task Brief: restate the request, list scope and out-of-scope items. Then add a required "## Acceptance Criteria" Markdown table with the columns `ID | Requirement | Risk (H/M/L)` — one row per testable requirement, each with a stable ID (AC1, AC2, …). These IDs are the durable contract: later stages bind their plan and proof to them, so make each criterion concrete and verifiable. Also add an "## Open assumptions / interpretation decisions" section that diffs this normalized brief against the raw request — call out every place you inferred, narrowed, or reinterpreted, so nothing the user asked for silently disappears. This brief is aspirational, not a validation record. Read-only — do not modify any files.


Context artifacts available to you (ids): art_foqZYk64AO

## Output
Write your task_brief as clear Markdown. Then, as the LAST thing in your
response, emit a single fenced ```json block with a structured summary
of the task_brief (keys appropriate to the stage). The json block is parsed
and stored as a structured artifact.
