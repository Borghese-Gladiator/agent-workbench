===== stage=options_plan_test mode=stage-packet =====
taskId: task_UcDnY68hgr
contextArtifactIds: art_Cy_dCdu6mo, art_K47DXdioUU, art_qnKxK6eW4J, art_oAAmQzj2h0, art_mVgFYcBz8X
reviewerFeedback: (none)
skillText: (none)
----- PROMPT (6663 chars) -----
# Stage: options_plan_test

## Task
Title: Add divide with divide-by-zero handling

Request:
Add a divide(a, b) function to the calc library in src/calc.js that throws a clear error on divide-by-zero. Follow the existing style of the other functions.

## Your job
Produce an Execution Plan: options considered, the chosen approach, an ordered change list, and a test plan. The test plan MUST include a "## Validation by criterion" Markdown table with the columns `Criterion ID | Validation method | Test type | Automated?` — one row per Acceptance Criteria ID from the Task Brief, binding each criterion to how it will be proven (test type = unit/integration/e2e/manual; Automated? = yes/no). Every brief criterion must appear; if one genuinely cannot be validated, say so and why. Read-only — do not modify any files.

## Asking the operator
If this stage has a material fork the operator should decide — competing
approaches, a scope tradeoff, or a risk worth flagging — call the
`mcp__ask__workbench_ask` tool with a short `header`, the `question`, and 2–4
labelled `options` (each with a `description`); set `multiSelect: true` only if
more than one may apply. Wait for the answer, then continue. Ask only when it
genuinely changes the outcome — don't ask about things you can reasonably decide.

## Prior context
The following artifacts were produced by EARLIER stages of THIS task. They
are the authoritative record of what was already decided/understood — read
them and build on them; do NOT re-derive what they already cover.

### Task Brief — task_brief (claude)

I'll write the task brief directly as instructed by the stage, then write the plan file and exit plan mode.

---

# Task Brief: Add `divide(a, b)` to the calc library

## Request Summary

Add a `divide(a, b)` function to `src/calc.js` that performs division and throws a descriptive error when the divisor is zero. The function must follow the same code style (naming, error pattern, export mechanism) as the existing arithmetic functions already in the file.

---

## Scope

**In scope:**
- Add `divide(a, b)` to `src/calc.js`
- Throw an `Error` with a clear message when `b === 0`
- Export the function using the same pattern as existing functions
- Match existing style: indentation, naming conventions, function form (declaration vs expression vs arrow)

**Out of scope:**
- Modifying or touching any existing functions
- Input type validation (e.g., rejecting non-numeric arguments) beyond what existing functions already do
- Floating-point precision handling or rounding (e.g., `1/3` returns a repeating decimal as-is)
- Adding tests (not requested; left to a later stage if applicable)
- CLI, REPL, or UI integration

---

## Acceptance Criteria

| ID | Requirement | Risk |
|----|-------------|------|
| AC1 | `divide` is exported from `src/calc.js` and importable by callers | L |
| AC2 | `divide(6, 2)` returns `3` (correct integer division) | L |
| AC3 | `divide(a, 0)` throws an `Error` (or appropriate subclass) with a message that clearly identifies divide-by-zero | H |
| AC4 | `divide(0, 5)` returns `0` (zero numerator is valid and does not throw) | M |
| AC5 | `divide(7, 2)` returns `3.5` (fractional results are returned without rounding) | L |
| AC6 | `divide(-6, 2)` returns `-3` and `divide(6, -2)` returns `-3` (sign handling follows JS semantics) | L |
| AC7 | The function's style (form, formatting, error handling pattern) is consistent with the other functions in `src/calc.js` | M |

---

## Open Assumptions / Interpretation Decisions

The following are places where the raw request was underspecified and a decision was made to normalize it:

1. **Error type** — The request says "throws a clear error" without naming a class. Assumed `new Error(...)` unless the existing file already uses a subclass (e.g., `RangeError`). Will follow whatever convention is already present.

2. **Error message wording** — No exact message was given. Assumed something like `"Cannot divide by zero"` or `"Division by zero"`. Final wording will be chosen to match the tone and capitalization style of any existing error messages in the file.

3. **Floating-point results** — Not mentioned in the request. Assumed no rounding or precision normalization: `divide(1, 3)` yields `0.3333…` as native JS would. If rounding is expected, that must be explicitly added to scope.

4. **Non-numeric inputs** — The request does not ask for type guards. Assumed none will be added unless the existing functions already include them (in which case the same pattern would be followed per the "existing style" requirement).

5. **Export mechanism** — Could be CommonJS (`module.exports`) or ES module (`export`). Will mirror whatever the existing functions use; this is not a new decision.

6. **Tests** — The request does not mention tests. Treated as out of scope. If a test file exists and follows a pattern that should be extended, this should be raised as a separate task.

---

### Discovery — discovery (claude)

## Discovery

### File: `src/calc.js`

The only JavaScript file in the project. It contains three arithmetic functions.

**Code style observations:**
- **Module format**: ES module (`export function` syntax, no CommonJS `module.exports`)
- **Function form**: Named function declarations (`export function name(a, b) { ... }`)
- **Parameters**: Consistent two-parameter signature `(a, b)`
- **Body**: Single `return` statement, no input validation, no type guards
- **Error handling**: None of the existing functions throw errors — this is the first function that will need to
- **Formatting**: 2-space indentation, no trailing commas, no semicolons after closing braces
- **Error type to use**: No existing error throws to reference, so `new Error(...)` (base class) is the natural default unless scoped to `RangeError` (which would be semantically appropriate for divide-by-zero but is not an established pattern in this file)

**What `divide` needs to add:**
- A guard: `if (b === 0) throw new Error("Division by zero")`
- Return: `return a / b`
- Export: `export function divide(a, b) { ... }` matching the existing declaration style

No test files, no `package.json`, no other configuration files were found — the task is entirely self-contained in `src/calc.js`.

---

_All context artifact ids for this task: art_Cy_dCdu6mo, art_K47DXdioUU, art_qnKxK6eW4J, art_oAAmQzj2h0, art_mVgFYcBz8X._

## Output
Write your execution_plan as clear Markdown. Then, as the LAST thing in your
response, emit a single fenced ```json block with a structured summary
of the execution_plan (keys appropriate to the stage). The json block is parsed
and stored as a structured artifact.
