===== stage=discovery mode=stage-packet =====
taskId: task_FsY2cGMCvb
contextArtifactIds: art_9yKAX1FeM0, art_O2C13IztMk, art_JWfwoOWiZL
reviewerFeedback: (none)
skillText: (none)
----- PROMPT (7644 chars) -----
# Stage: discovery

## Task
Title: Add divide with divide-by-zero handling

Request:
Add a divide(a, b) function to the calc library in src/calc.js that throws a clear error on divide-by-zero. Follow the existing style of the other functions.

## Your job
Do Discovery: explore the codebase to find the files, modules, and conventions relevant to this task. Read-only — do not modify any files.


## Prior context
The following artifacts were produced by EARLIER stages of THIS task. They
are the authoritative record of what was already decided/understood — read
them and build on them; do NOT re-derive what they already cover.

### Task Brief — task_brief (claude)

---

## Task Brief: Add `divide(a, b)` to calc library

### Request Summary

Add a `divide(a, b)` function to `src/calc.js` that performs standard arithmetic division and throws a clear, descriptive error when the divisor `b` is zero. The implementation must follow the existing style of the other arithmetic functions already present in the file.

---

### Scope

**In scope:**
- Define and export `divide(a, b)` in `src/calc.js`
- Throw a `Error` with a descriptive message when `b === 0`
- Match the code style (function declaration form, export mechanism, naming conventions) of existing functions

**Out of scope:**
- Modifying or refactoring existing functions
- Adding tests (unless a test file already exists and a pattern is established)
- Input validation for non-numeric types (NaN, strings) beyond what other functions do
- Floating-point special cases (Infinity, NaN) beyond the zero check

---

## Acceptance Criteria

| ID | Requirement | Risk (H/M/L) |
|----|-------------|--------------|
| AC1 | `divide(a, b)` is defined in `src/calc.js` and exported consistent with existing exports | L |
| AC2 | `divide(10, 2)` returns `5` (basic integer division) | L |
| AC3 | `divide(7, 2)` returns `3.5` (non-integer quotient returned correctly) | L |
| AC4 | `divide(0, 5)` returns `0` (zero numerator is valid and does not throw) | M |
| AC5 | `divide(a, 0)` throws an error for any value of `a` | H |
| AC6 | The error thrown for divide-by-zero has a clear, human-readable message (e.g. `"Cannot divide by zero"`) | M |
| AC7 | The function signature, declaration style, and export match the pattern of existing functions in `src/calc.js` | L |

---

## Open Assumptions / Interpretation Decisions

1. **Error type** — "throws a clear error" is interpreted as `throw new Error(...)` using a standard JavaScript `Error` object. If the existing codebase uses a custom error class or a different throw pattern, that pattern should be followed instead (AC6 and AC5 still hold for message clarity).

2. **Zero check semantics** — JavaScript's `-0 === 0` evaluates to `true`, so `divide(5, -0)` will also throw under a strict `=== 0` check. This is treated as correct behavior and not a bug. Not called out in the request but covered implicitly by AC5.

3. **Negative numbers** — The request does not mention negative operands. Assumed to work via standard JS division (no special handling needed).

4. **Export style** — Not specified in the request. Assumed to mirror whatever mechanism (`module.exports`, named `export`, etc.) the existing functions use.

5. **Non-numeric inputs** — No mention of guarding against `null`, `undefined`, strings, etc. Assumed out of scope, consistent with the style of existing functions.

6. **No test requirement stated** — The request does not ask for tests. If a test file (e.g., `src/calc.test.js`) already exists, adding corresponding tests would be natural but is treated as out of scope unless confirmed.

---

```json
{
  "title": "Add divide(a, b) with divide-by-zero handling to calc library",
  "scope": {
    "in_scope": [
      "Define and export divide(a, b) in src/calc.js",
      "Throw a descriptive Error when b === 0",
      "Match existing function style and export pattern"
    ],
    "out_of_scope": [
      "Modifying existing functions",
      "Adding tests (unless existing test file establishes a clear pattern)",
      "Non-numeric input validation",
      "Floating-point edge cases beyond zero check"
    ]
  },
  "acceptance_criteria": [
    { "id": "AC1", "requirement": "divide(a, b) is defined in src/calc.js and exported consistently with existing exports", "risk": "L" },
    { "id": "AC2", "requirement": "divide(10, 2) returns 5", "risk": "L" },
    { "id": "AC3", "requirement": "divide(7, 2) returns 3.5", "risk": "L" },
    { "id": "AC4", "requirement": "divide(0, 5) returns 0 without throwing", "risk": "M" },
    { "id": "AC5", "requirement": "divide(a, 0) throws an error for any value of a", "risk": "H" },
    { "id": "AC6", "requirement": "Error thrown on divide-by-zero has a clear human-readable message", "risk": "M" },
    { "id": "AC7", "requirement": "Function style matches existing functions in src/calc.js", "risk": "L" }
  ],
  "open_assumptions": [
    "Error type is standard JS Error unless existing code uses a custom class",
    "Negative zero (-0) will also trigger the zero check (treated as correct)",
    "Negative number operands work via standard JS division with no special handling",
    "Export style mirrors existing functions (not specified in request)",
    "Non-numeric input validation is out of scope",
    "Tests are out of scope unless an existing test file is present"
  ]
}
```

## Structured summary

```json
{
  "title": "Add divide(a, b) with divide-by-zero handling to calc library",
  "scope": {
    "in_scope": [
      "Define and export divide(a, b) in src/calc.js",
      "Throw a descriptive Error when b === 0",
      "Match existing function style and export pattern"
    ],
    "out_of_scope": [
      "Modifying existing functions",
      "Adding tests (unless existing test file establishes a clear pattern)",
      "Non-numeric input validation",
      "Floating-point edge cases beyond zero check"
    ]
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "requirement": "divide(a, b) is defined in src/calc.js and exported consistently with existing exports",
      "risk": "L"
    },
    {
      "id": "AC2",
      "requirement": "divide(10, 2) returns 5",
      "risk": "L"
    },
    {
      "id": "AC3",
      "requirement": "divide(7, 2) returns 3.5",
      "risk": "L"
    },
    {
      "id": "AC4",
      "requirement": "divide(0, 5) returns 0 without throwing",
      "risk": "M"
    },
    {
      "id": "AC5",
      "requirement": "divide(a, 0) throws an error for any value of a",
      "risk": "H"
    },
    {
      "id": "AC6",
      "requirement": "Error thrown on divide-by-zero has a clear human-readable message",
      "risk": "M"
    },
    {
      "id": "AC7",
      "requirement": "Function style matches existing functions in src/calc.js",
      "risk": "L"
    }
  ],
  "open_assumptions": [
    "Error type is standard JS Error unless existing code uses a custom class",
    "Negative zero (-0) will also trigger the zero check (treated as correct)",
    "Negative number operands work via standard JS division with no special handling",
    "Export style mirrors existing functions (not specified in request)",
    "Non-numeric input validation is out of scope",
    "Tests are out of scope unless an existing test file is present"
  ]
}
```

_All context artifact ids for this task: art_9yKAX1FeM0, art_O2C13IztMk, art_JWfwoOWiZL._

## Output
Write your discovery as clear Markdown. Then, as the LAST thing in your
response, emit a single fenced ```json block with a structured summary
of the discovery (keys appropriate to the stage). The json block is parsed
and stored as a structured artifact.
