===== stage=discovery mode=stage-packet =====
taskId: task_kw4k7ANF9K
contextArtifactIds: art_CnFQYmBfuH, art_0YvQD22YpN, art_R73O8zgTVV
reviewerFeedback: (none)
skillText: (none)
----- PROMPT (1958 chars) -----
# Stage: discovery

## Task
Title: Add divide with divide-by-zero handling

Request:
Add a divide(a, b) function to the calc library in src/calc.js that throws a clear error on divide-by-zero. Follow the existing style of the other functions.

## Your job
Do Discovery: find the files, modules, and conventions relevant to this task and report only what the implementer needs to know (where to change, what pattern to follow, what to watch out for). A small change warrants a few sentences, not a formatted report. Read-only — do not modify any files.


## Prior context
The following artifacts were produced by EARLIER stages of THIS task. They
are the authoritative record of what was already decided/understood — read
them and build on them; do NOT re-derive what they already cover.

### Task Brief — task_brief (claude)

Without filesystem tools in this session, I'll work from the task description directly — the request is unambiguous enough that no exploration is needed.

---

## Task Brief: Add `divide(a, b)` to calc library

Add a `divide(a, b)` function to `src/calc.js` matching the existing function style, with divide-by-zero protection.

## Acceptance Criteria

| ID  | Requirement | Risk |
|-----|-------------|------|
| AC1 | `divide(a, b)` returns the correct quotient `a / b` for valid numeric inputs | L |
| AC2 | Calling `divide(a, 0)` throws an error with a clear, descriptive message | M |

## Scope note

Style conformance (naming, export pattern, argument handling) is implicitly required but is not a separate user-visible goal — it is a constraint on how AC1 is delivered.

_All context artifact ids for this task: art_CnFQYmBfuH, art_0YvQD22YpN, art_R73O8zgTVV._

## Output
Write your discovery as clear Markdown. Then, as the LAST thing in your
response, emit a single fenced ```json block with a structured summary
of the discovery (keys appropriate to the stage). The json block is parsed
and stored as a structured artifact.
