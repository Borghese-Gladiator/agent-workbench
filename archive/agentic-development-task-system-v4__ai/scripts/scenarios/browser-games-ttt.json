{
  "description": "Build tic-tac-toe INSIDE the existing /Users/timothy.shee/GitHub/wip-browser-games repo (not a throwaway). create:true registers the project at that path; wipe:false uses the repo as-is (the build lands on a worktree branch and is squash-merged into local main). Records a video walking the gates + showing the game.",
  "defaultMode": "record",
  "resultMode": "play",
  "project": {
    "create": true,
    "wipe": false,
    "name": "WIP Browser Games",
    "target": "/Users/timothy.shee/GitHub/wip-browser-games",
    "defaultBranch": "main",
    "agentRuntime": "claude",
    "deliveryPolicy": "merge_to_master",
    "devCommand": "npx --yes serve -l 5173 .",
    "testCommand": "npm test --silent || true",
    "typecheckCommand": "npx --yes tsc --noEmit || true"
  },
  "task": {
    "title": "Build a playable tic-tac-toe game",
    "request": [
      "Build a tic-tac-toe game as a single static web app at the root of this repo",
      "(index.html + a JS file, no framework, no build step) that two players can",
      "play in the browser:",
      "- A 3x3 grid the players click to place X then O, alternating.",
      "- Detect and announce the winner (or a draw), and offer a reset.",
      "- Use accessible, user-facing markup so a Playwright test can drive it with",
      "  getByRole.",
      "",
      "In the QA/validation stage, write and run a Playwright end-to-end test that",
      "plays a full game to a win in a real browser, with video + trace ON, and",
      "leave the recording as proof."
    ]
  }
}
